/*const users = [];
//camelCase

//const add = function()[]
const add = (user) => {
    users.push(user)
}

const remove = (a) => {
    return users.filter(user) => {
        user.id !== id
    }
    
}
*/

// import individualmente
import remove, {add, VARIABLE} from "./node js/functions"
// esto importa todo lo que se exporto de ese archivo
import * as Validations from "./node js/functions";